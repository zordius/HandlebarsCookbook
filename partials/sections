{{#main_section}}
 {{#eq @section "lcsample"}}
  {{>lcsample}}
 {{else eq @section "lccompile"}}
  {{>lccompile}}
 {{else}}
<h2>{{@section}}</h2>
  {{#if (lookup (lookup . @section) "toc")}}
   {{> toc}}
  {{else if (lookup (lookup . @section) "version")}}
   {{> versions}}
  {{else}}
   {{isStringThenOutput . tag='p'}}
   {{>section_columns}}
  {{/if}}
 {{/eq}}
{{/main_section}}
