title: Simple Variable
description:
  Use <code>{{foo}}</code> will lookup the <code>foo</code> value from the input data.
Samples:
  quicksample:
    - template: "{{foo}}"
      data:
        foo: Hello
    - template: "{{foo}}"
      data:
        bar: missing
