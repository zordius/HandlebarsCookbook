title: Simple Variable
description:
  Use <code>{{foo}}</code> will lookup the <code>foo</code> value from the input data. The value will be escaped by default.
Samples:
  quicksample:
    - data:
      foo: Hello
    - template: {{foo}}
