title: Raw Output
description:
  Use output of <code>{{{foo}}}</code> or <code>{{&foo}}</code> will not escaped by template engine.
Samples:
  quicksample:
    - template: "{{{foo}}}"
      data:
        foo: Hello & Happy
    - template: "{{&foo}}"
      data:
        foo: <p>Hello</p>
